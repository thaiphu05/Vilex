# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Logs
*.log

# Environments
.venv/
venv/
.venv-tts/

# Pipeline outputs / local data
outputs/
output/
results/
data-annotations/
data-dialogues/

# Secrets / API credentials (NEVER commit these)
.env
.env.*
*.pem
*.key
*.crt
*.p12
*.token
*api*key*
*credentials*.json
*credential*.json
*service-account*.json
*service_account*.json
*sa.json
gen-lang-client*.json
compact-record-*.json
credentials.json

# Generated speech audio (e.g. convert_spoken.py --save_dir audios_chatterbox)
*.wav
*.mp3
*.flac
*.m4a
*.opus
*.ogg

# ...but DO ship the intentional TTS prompt/reference wavs.
!tts_render/prompt_wavs/
!tts_render/prompt_wavs/*.wav
!tts_render/prompt_wavs/

# Personal voice-clone samples (never commit user voice recordings/transcripts)
voice_clone/

# LibriSpeech sample corpus (public dataset, large/no need to ship)
tts_render/librispeech_samples/

# OS junk
.DS_Store
Thumbs.db

# Coverage and build artifacts
.coverage
.coverage.*
htmlcov/
build/
dist/

predictions/
session-*.md
